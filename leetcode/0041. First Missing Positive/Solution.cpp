#include <algorithm>
#include <iostream>
#include <stack>
#include <vector>
using namespace std;

class Solution {
public:
  int firstMissingPositive(first<int> &nums) {
    int ans;
    for (int i = 0; i < nums.size(); ++i) {
    }
    return ans;
  }
};
